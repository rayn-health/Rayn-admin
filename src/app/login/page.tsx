import { Suspense } from "react";
import { GoogleSignInButton } from "@/components/auth/GoogleSignInButton";
export default function LoginPage(){return <main className="min-h-screen flex flex-col items-center justify-center gap-8 px-6"><div className="text-center"><p className="font-mono text-xs uppercase tracking-[0.25em] text-signal-soft mb-2">Rayn Admin</p><h1 className="font-display text-3xl text-ink-100">Sign in to continue</h1><p className="text-ink-500 text-sm mt-2 max-w-xs">Restricted to the authorised Google account and enforced by Firebase.</p></div><Suspense fallback={null}><GoogleSignInButton/></Suspense></main>;}
