"use client";
import {useEffect,useState} from "react";
import {collection,addDoc,onSnapshot,orderBy,query,deleteDoc,doc,updateDoc,serverTimestamp} from "firebase/firestore";
import {db} from "@/lib/firebase/client";
import {Card} from "@/components/ui/Card";
type Project={id:string;title:string;status:'draft'|'active'|'archived';summary:string};
export default function ProjectsPage(){const [projects,setProjects]=useState<Project[]>([]);const [title,setTitle]=useState('');const [summary,setSummary]=useState('');useEffect(()=>onSnapshot(query(collection(db,'projects'),orderBy('createdAt','desc')),s=>setProjects(s.docs.map(d=>({id:d.id,title:d.data().title,status:d.data().status??'draft',summary:d.data().summary??''}))),[]);async function add(){if(!title.trim())return;await addDoc(collection(db,'projects'),{title:title.trim(),summary:summary.trim(),status:'draft',createdAt:serverTimestamp()});setTitle('');setSummary('')}async function cycle(p:Project){const next={draft:'active',active:'archived',archived:'draft'} as const;await updateDoc(doc(db,'projects',p.id),{status:next[p.status]})}return <div className="max-w-2xl"><h1 className="font-display text-2xl text-ink-100 mb-1">Projects</h1><p className="text-ink-500 text-sm mb-6">Manage website projects.</p><Card className="mb-6 grid gap-3"><input value={title} onChange={e=>setTitle(e.target.value)} placeholder="Project title" className="rounded bg-ink-800 p-2 text-ink-100"/><input value={summary} onChange={e=>setSummary(e.target.value)} placeholder="Summary" className="rounded bg-ink-800 p-2 text-ink-100"/><button onClick={add} className="rounded bg-signal px-4 py-2 text-ink-950 justify-self-start">Add project</button></Card>{projects.map(p=><Card key={p.id} className="mb-2 flex justify-between"><div><p className="text-ink-100">{p.title}</p><p className="text-ink-500 text-xs">{p.summary}</p></div><div className="flex gap-3"><button onClick={()=>cycle(p)} className="text-signal text-xs">{p.status}</button><button onClick={()=>deleteDoc(doc(db,'projects',p.id))} className="text-rose text-xs">Delete</button></div></Card>)}</div>}
